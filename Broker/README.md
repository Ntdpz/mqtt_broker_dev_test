# 🚀 Simple MQTT Broker

## 📖 คำอธิบาย

Simple MQTT Broker สำหรับมือใหม่ที่ต้องการเรียนรู้ MQTT

### ✨ ความสามารถ
- 📨 รับ-ส่งข้อความ MQTT
- 📂 จัดการ Topic ต่างๆ
- 📊 แสดงสถิติการทำงาน
- 📝 บันทึกกิจกรรมทั้งหมด
- ⚙️ ปรับแต่งได้ผ่าน config.json

## 🔧 การติดตั้ง

1. **ตรวจสอบ Python**
   ```cmd
   python --version
   ```

2. **ติดตั้ง dependencies (ถ้าจำเป็น)**
   ```cmd
   pip install socket threading json
   ```

## 🚀 วิธีใช้งาน

### เริ่มต้น Broker
```cmd
# Windows
start_broker.bat

# หรือใช้ Python โดยตรง
python simple_broker.py
```

### ตั้งค่า (config.json)
```json
{
  "broker": {
    "host": "localhost",
    "port": 1883
  },
  "logging": {
    "level": "INFO",
    "console": true,
    "file": true
  }
}
```

## 📨 รูปแบบข้อความ

Broker นี้ใช้ JSON format แทน MQTT Protocol จริง (เพื่อความง่าย):

### Publish ข้อความ
```json
{
  "type": "publish",
  "topic": "sensor/temperature",
  "payload": "25.5",
  "qos": 0
}
```

### Subscribe Topic
```json
{
  "type": "subscribe",
  "topic": "sensor/temperature"
}
```

### Ping
```json
{
  "type": "ping"
}
```

## 📊 ข้อมูลที่แสดง

- 🔗 จำนวนการเชื่อมต่อ
- 📨 จำนวนข้อความ
- 📂 Topic ที่ใช้งาน
- ⏱️ เวลาทำงาน

## 📁 ไฟล์ที่สำคัญ

- `simple_broker.py` - โค้ดหลักของ Broker
- `config.json` - ไฟล์ตั้งค่า
- `config_manager.py` - จัดการ config
- `start_broker.bat` - สคริปต์เริ่มต้น (Windows)
- `broker.log` - ไฟล์ log (จะสร้างอัตโนมัติ)

## 🔧 การปรับแต่ง

### เปลี่ยน Port
แก้ไขใน `config.json`:
```json
{
  "broker": {
    "port": 1884
  }
}
```

### เปลี่ยน Log Level
```json
{
  "logging": {
    "level": "DEBUG"
  }
}
```

## ❓ การแก้ไขปัญหา

### Port ถูกใช้แล้ว
```
Error: [Errno 10048] Only one usage of each socket address
```
**วิธีแก้:** เปลี่ยน port ใน config.json

### Python ไม่พบ
```
'python' is not recognized
```
**วิธีแก้:** ติดตั้ง Python และเพิ่มใน PATH

## 📚 ข้อมูลเพิ่มเติม

- MQTT Protocol: http://mqtt.org/
- Python Socket: https://docs.python.org/3/library/socket.html

---
สร้างด้วย ❤️ สำหรับผู้เริ่มต้นเรียนรู้ MQTT