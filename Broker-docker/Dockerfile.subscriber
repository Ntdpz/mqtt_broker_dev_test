# 🐳 Dockerfile สำหรับ MQTT Subscriber
FROM python:3.11-alpine

# 📦 ติดตั้ง dependencies พื้นฐาน
RUN apk update && apk add --no-cache \
    gcc \
    musl-dev \
    linux-headers

# 👤 สร้าง non-root user
RUN addgroup -g 1001 subscriber && \
    adduser -D -s /bin/sh -u 1001 -G subscriber subscriber

# 📁 สร้าง working directory
WORKDIR /app

# 📄 Copy requirements และ install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 📋 Copy subscriber code
COPY mqtt_subscriber.py ./subscriber.py

# 🔧 สร้าง directories สำหรับ logs
RUN mkdir -p /app/logs && \
    chown -R subscriber:subscriber /app

# 🔐 เปลี่ยนเป็น non-root user
USER subscriber

# 🚀 เริ่มต้น subscriber
CMD ["python", "subscriber.py"]